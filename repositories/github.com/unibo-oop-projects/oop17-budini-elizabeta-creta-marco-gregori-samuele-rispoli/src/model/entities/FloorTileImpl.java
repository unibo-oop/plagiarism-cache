package model.entities;

import java.awt.Dimension;

public class FloorTileImpl extends EntityImpl implements FloorTile {

    public FloorTileImpl(Double x, Double y, Dimension dim) {
        super(x, y, dim);
    }

}
