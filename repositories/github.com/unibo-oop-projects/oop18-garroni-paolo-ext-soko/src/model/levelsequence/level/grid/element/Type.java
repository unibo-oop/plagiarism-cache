package model.levelsequence.level.grid.element;

/**
 * The Type of the elements.
 */
public enum Type {

    /** The user type. */
    USER,
    /** The box type. */
    BOX,
    /** The wall type. */
    WALL,
    /** The target type. */
    TARGET,
    /** The empty type. */
    EMPTY;
}
