package spacesurvival.view.settings;

/**
 * Implements the function that creates the graphics's settings GUI.
 */
public interface FactoryGUISettings {

    /**
     * Create a graphics's settings GUI.
     * @return settings GUI.
     */
    GUISettings create();
}
