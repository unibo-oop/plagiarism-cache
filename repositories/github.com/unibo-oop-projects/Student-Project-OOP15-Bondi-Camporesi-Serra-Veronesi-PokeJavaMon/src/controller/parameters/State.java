package controller.parameters;

/**
 * This enum contains all the states in which player can be
 */
public enum State {
    FIRST_MENU, 
    SECOND_MENU, 
    WALKING, 
    MENU, 
    FIGHTING, 
    READING;
}
