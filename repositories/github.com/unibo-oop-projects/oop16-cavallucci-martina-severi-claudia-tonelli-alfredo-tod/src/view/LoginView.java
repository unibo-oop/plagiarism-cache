package view;
/**
 * The Login interface that permits to the user to enter in a personal profile.
 */
public interface LoginView extends View {

    /**
     * Cleans all textField in the panel.(The username and the password).
     */
    void clearLogin();

}
