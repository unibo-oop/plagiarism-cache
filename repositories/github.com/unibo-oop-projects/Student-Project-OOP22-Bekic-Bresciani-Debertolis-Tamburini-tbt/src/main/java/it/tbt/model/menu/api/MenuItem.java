package it.tbt.model.menu.api;

/**
 * The {@code MenuItem} interface represents an item in a menu.
 */
public interface MenuItem {
    /**
     * Returns the text of the menu item.
     *
     * @return the text of the menu item
     */
    String getText();
}
