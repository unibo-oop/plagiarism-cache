package it.unibo.jetpackjoyride.menu.buttoncommand.api;

/**
 * This interface represents a command used for implementing the Command pattern.
 * @author yukai.zhou@studio.unibo.it
 */
public interface Command {
    /**
     * A method that executes the command.
     */
    void execute();
}
