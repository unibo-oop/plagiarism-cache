package controller.utilities;

/**
 * The entire list of score messages.
 */
public enum ScoreData {

    /**
     * The message returned when the highest score changed.
     */
    RECORD,
    
    /**
     * The message returned when the last scores changed.
     */
    LAST_SCORES;

}
