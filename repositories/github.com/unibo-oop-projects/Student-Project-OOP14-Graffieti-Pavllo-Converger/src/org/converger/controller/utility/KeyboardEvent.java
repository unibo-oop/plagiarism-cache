package org.converger.controller.utility;

/**
 * Represents all the key event that the application have to manage.
 * Now the enum has only one value, but in future we will added more keyboard events
 * @author Gabriele Graffieti
 *
 */
public enum KeyboardEvent {
	/** ctrl + c event. */
	COPY;
}
