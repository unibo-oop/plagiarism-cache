package it.unibo.project.controller.core.api;

/**
 * enum {@code Difficulty} lists all difficulties player can choose from.
 */
public enum Difficulty {
    /**
     * easy mode.
     */
    EASY,
    /**
     * normal mode (default one).
     */
    NORMAL,
    /**
     * hard mode.
     */
    HARD,
}
