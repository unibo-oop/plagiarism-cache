package home.view.fx.button;

/**
 * a simple button inside the menu.
 */
public interface MenuButton {
    /**
     * set the text of menu button when menu is repainted.
     * @param text
     *      the text of button
     */
    void setText(String text);
}
