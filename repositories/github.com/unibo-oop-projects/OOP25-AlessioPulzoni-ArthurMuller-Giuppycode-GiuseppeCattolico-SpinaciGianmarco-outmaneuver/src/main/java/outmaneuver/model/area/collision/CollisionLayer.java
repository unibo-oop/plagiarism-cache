package outmaneuver.model.area.collision;

/** Classifies entities by collision purpose, used to filter which collisions are relevant. */
public enum CollisionLayer {
    PLANE,
    MISSILE,
    COLLECTIBLE
}
