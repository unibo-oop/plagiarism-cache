package controller;

public interface CinemaController {
    /**
     * Show menu.
     */
    void showMenu();
    /**
     * Initialize application when is started.
     * @param path where initialize application
     */
    void initApplication(String path);
}
