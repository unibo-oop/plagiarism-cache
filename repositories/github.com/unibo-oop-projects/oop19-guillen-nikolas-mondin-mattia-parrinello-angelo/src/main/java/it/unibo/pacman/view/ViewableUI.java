package it.unibo.pacman.view;
/**
 * An interface for showing and closing the GUI.
 */
public interface ViewableUI {

    /**
     * Makes the component visible.
     */
    void show();

    /**
     * Makes the component invisible.
     */
    void close();
}
