package model.domain;

/**
 * Enumeration of available permanent buffs.
 */
public enum BuffType {
    EXTRA_LIVES,
    FIRST_ERROR_PROTECT,
    STARTING_CELLS,
    POINT_BONUS,
    INVENTORY_CAPACITY;
}
